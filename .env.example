# ============================================
# CredVault Environment Configuration
# ============================================
# Copy this file to .env.local and fill in your actual values

# ============================================
# NextAuth Configuration
# ============================================
# Your application URL (for development, use localhost)
NEXTAUTH_URL=http://localhost:4300

# Secret key for NextAuth session encryption
# Generate a secure random string (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secure-secret-key-here-minimum-32-characters

# ============================================
# MongoDB Configuration
# ============================================
# Local MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/credvault

# For MongoDB Atlas (cloud), use this format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/credvault?retryWrites=true&w=majority

# For MongoDB with authentication:
# MONGODB_URI=mongodb://username:password@localhost:27017/credvault?authSource=admin

# ============================================
# OAuth Providers (Optional)
# ============================================
# These are optional - leave empty if not using OAuth
# Get credentials from:
# - Google: https://console.cloud.google.com/apis/credentials
# - GitHub: https://github.com/settings/developers

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================
# Blockchain Configuration
# ============================================
# BlockScout Explorer URL for viewing blockchain transactions
NEXT_PUBLIC_BLOCKSCOUT_URL=http://localhost:26000

# ============================================
# Additional Configuration (Future)
# ============================================
# Database URL (if using separate database connection)
# DATABASE_URL=postgresql://user:password@localhost:5432/credvault

# ============================================
# VAULT Protocol Configuration
# ============================================
# VAULT Protocol API URL (blockchain-based certificate storage)
# Default: http://localhost:3001
VAULT_PROTOCOL_URL=http://localhost:3001

# File encryption key for VAULT Protocol (AES-256-GCM)
# Generate using: openssl rand -hex 32
# This must be a 64-character hex string (32 bytes)
FILE_ENCRYPTION_KEY=generate-32-byte-hex-key-here

# IPFS Configuration (used by VAULT Protocol)
IPFS_URL=http://127.0.0.1:5001

# Blockchain Configuration (Quorum - used by VAULT Protocol)
BLOCKCHAIN_URL=http://127.0.0.1:8545
CHAIN_ID=1337

# ============================================
# VAULT Protocol Setup Instructions
# ============================================
# 1. Clone the VAULT Protocol repository:
#    git clone https://github.com/sarthakpriyadarshi/VaultProtocol.git
#
# 2. Follow the setup instructions in VaultProtocol README
#
# 3. Start the VAULT Protocol services:
#    - IPFS: ipfs daemon
#    - Quorum: npx quorum-dev-quickstart (follow the prompts)
#    - VAULT API: npm run dev:encrypted
#
# 4. Update the URLs above to match your VAULT Protocol configuration

# Email service configuration (for future email verification)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ============================================
# Email Configuration (Nodemailer)
# ============================================
# SMTP server configuration for sending emails
# For iCloud+ with custom domain:
# - SMTP_USER must be your @icloud.com / @me.com / @mac.com email (for authentication)
# - EMAIL_FROM_ADDRESS can be your custom domain email (for sending)
# For Gmail: smtp.gmail.com (requires app-specific password)
# For Outlook: smtp-mail.outlook.com
# For SendGrid: smtp.sendgrid.net
# For custom SMTP: your-smtp-server.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email sender information
EMAIL_FROM_NAME=CredVault
EMAIL_FROM_ADDRESS=noreply@credvault.app

# ============================================
# Gmail App Password Setup Instructions
# ============================================
# To use Gmail for sending emails:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and your device
# 4. Generate an app password
# 5. Use the generated 16-character password for SMTP_PASS
#
# Note: Never use your regular Gmail password!

# ============================================
# Admin Configuration (Optional - for secure admin creation)
# ============================================
# Add this secret to protect the admin creation endpoint
# Generate a secure random string (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
ADMIN_CREATE_SECRET=your-secure-admin-creation-secret-here

# ============================================
# Production Environment Variables
# ============================================
# For production deployment, ensure you:
# 1. Use strong, unique values for all secrets
# 2. Use MongoDB Atlas or secure MongoDB instance
# 3. Configure OAuth applications for your domain
# 4. Use HTTPS URLs for NEXTAUTH_URL
# 5. Secure your VAULT Protocol endpoints
#
# Example production NEXTAUTH_URL:
# NEXTAUTH_URL=https://your-domain.vercel.app
#
# Example MongoDB Atlas connection:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/credvault?retryWrites=true&w=majority
