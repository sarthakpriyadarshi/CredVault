/**
 * Next.js font configuration (client-side)
 * Used for web font loading in the browser
 */

import { Inter } from "next/font/google"

export const geist = Inter({
  subsets: ["latin"],
  display: "swap",
})

